<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Ingrediente</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Editar Ingrediente</h1>
    <form action="/atualizar-ingrediente/<%= ingrediente.id %>" method="POST">
      <div class="mb-3">
        <label for="comida_id" class="form-label">Receita ID</label>
        <input type="number" class="form-control" id="comida_id" name="comida_id" value="<%= ingrediente.comida_id %>" required>
      </div>
      <div class="mb-3">
        <label for="descricao" class="form-label">Descrição</label>
        <input type="text" class="form-control" id="descricao" name="descricao" value="<%= ingrediente.descricao %>" required>
      </div>
      <div class="mb-3">
        <label for="quantidade" class="form-label">Quantidade</label>
        <input type="text" class="form-control" id="quantidade" name="quantidade" value="<%= ingrediente.quantidade %>" required>
      </div>
      <div class="mb-3">
        <label for="unidade" class="form-label">Unidade</label>
        <input type="text" class="form-control" id="unidade" name="unidade" value="<%= ingrediente.unidade %>" required>
      </div>
      <button type="submit" class="btn btn-primary">Atualizar</button>
      <a href="/listar-ingredientes" class="btn btn-secondary">Voltar</a>
    </form>
  </div>
</body>
</html>
