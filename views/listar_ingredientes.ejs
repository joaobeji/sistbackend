<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Listar Ingredientes</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4">Lista de Ingredientes</h1>
    <table class="table">
      <thead>
        <tr>
          <th>ID</th>
          <th>Receita ID</th>
          <th>Descrição</th>
          <th>Quantidade</th>
          <th>Unidade</th>
        </tr>
      </thead>
      <tbody>
        <% ingredientes.forEach(ingrediente => { %>
          <tr>
            <td><%= ingrediente.id %></td>
            <td><%= ingrediente.comida_id %></td>
            <td><%= ingrediente.descricao %></td>
            <td><%= ingrediente.quantidade %></td>
            <td><%= ingrediente.unidade %></td>
            <td>
              <a href="/editar-ingrediente/<%= ingrediente.id %>" class="btn btn-warning">Editar</a>
              <form action="/deletar-ingrediente/<%= ingrediente.id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn btn-danger" onclick="return confirm('Tem certeza que deseja deletar este ingrediente?');">Deletar</button>
              </form>
            </td>
          </tr>
        <% }); %>
      </tbody>
      
    </table>
    <a href="/dashboard" class="btn btn-secondary">Voltar para a Dashboard</a>
  </div>
</body>
</html>
